name: ai_agent_app
description: AI Agent — Universal content creation assistant
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.0
  image_picker: ^1.1.0
  file_picker: ^8.0.0
  path_provider: ^2.1.0
  flutter_markdown: ^0.7.0
  cached_network_image: ^3.4.0
  share_plus: ^10.0.0
  url_launcher: ^6.3.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
